# 순회 인덱스 — 반복 인덱스, 값 를 목록 에서
메뉴 = ["김밥", "라면", "돈까스"]
반복 번호, 음식 를 메뉴 에서 {
    출력(서식("{}. {}", 번호 + 1, 음식))
}

# 인덱스로 짝수 번째만
반복 i, 글자 를 ["가", "나", "다", "라"] 에서 {
    만약 i % 2 == 0 {
        출력(글자)
    }
}
