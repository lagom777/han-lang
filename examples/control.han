# 멈춤(break) · 계속(continue)
# 1~20 중 짝수는 건너뛰고(계속), 누적합이 50을 넘으면 멈춤(break)
합 = 0
반복 i 를 1 부터 20 까지 {
    만약 i % 2 == 0 {
        계속
    }
    합 = 합 + i
    만약 합 > 50 {
        출력("합이 " + 합 + " — 멈춤 (i=" + i + ")")
        멈춤
    }
}
출력("최종 합: " + 합)
