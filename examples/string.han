# 문자열·타입 내장함수
출력(대문자("han"))
출력(다듬기("  한 언어  "))
출력(바꾸기("가나다나", "나", "_"))
출력("'글' 포함? " + 포함("한글날", "글"))
출력("타입: " + 타입([1, 2, 3]))

# 나누기 + 시작 조합
반복 단어 를 나누기("사과,배,사탕", ",") 에서 {
    만약 시작(단어, "사") {
        출력(단어 + " — 사로 시작해요")
    }
}
