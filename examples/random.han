# 무작위 — 게임·샘플링용 (실행할 때마다 결과가 달라요)
출력("주사위: " + 무작위정수(1, 6))
출력("동전: " + 무작위선택(["앞", "뒤"]))

색들 = ["빨강", "초록", "파랑"]
출력("오늘의 색: " + 무작위선택(색들))

# 무작위 확률(0~1)로 분기
만약 무작위() < 0.5 {
    출력("운이 좋네요!")
} 아니면 {
    출력("다음 기회에...")
}
