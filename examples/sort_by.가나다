# 정렬기준 — 키 함수로 정렬 (구조화 데이터 처리)
점수표 = [
    {"이름": "민지", "점수": 85},
    {"이름": "현우", "점수": 92},
    {"이름": "서연", "점수": 78}
]

# 점수 높은 순 (오름차순 정렬 후 거꾸로)
순위 = 거꾸로(정렬기준(점수표, 람다(학생) { 반환 학생["점수"] }))
반복 학생 를 순위 에서 {
    출력(서식("{} — {}점", 학생["이름"], 학생["점수"]))
}
