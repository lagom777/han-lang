# 환경변수 읽기 — 설정·비밀키
키 = 환경변수("OPENROUTER_API_KEY", "(미설정)")
만약 키 == "(미설정)" {
    출력("OPENROUTER_API_KEY 가 없어요 — AI는 stub로 동작")
} 아니면 {
    출력("API 키 감지됨")
}

집 = 환경변수("HOME", "(모름)")
출력("HOME: " + 집)
