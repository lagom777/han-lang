# 자료 — 방명록 저장소 (자료 관심사만: 어디에 어떻게 저장하는지는 이 파일이 다 안다)
# 앱.ㄱㄴㄷ 가 모듈("자료.ㄱㄴㄷ") 로 불러 쓴다. 경로·시험용으로 환경변수 주입 가능.
방명록 = 저장소(환경변수("자료경로", "방명록.db"), "글들")

함수 글넣기(이름, 내용) {
    반환 넣기(방명록, {"이름": 이름, "내용": 내용})
}

함수 글모두() {
    반환 모두(방명록)
}
