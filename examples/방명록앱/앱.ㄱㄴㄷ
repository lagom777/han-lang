# 앱 — 자료·화면 모듈을 엮어 라우트만 정의 (관심사 분리: 자료/화면/앱, 전부 한 언어)
#   ./가나다 실행 examples/방명록앱/앱.ㄱㄴㄷ  →  http://localhost:8000
자료 = 모듈("자료.ㄱㄴㄷ")
화면 = 모듈("화면.ㄱㄴㄷ")

함수 첫화면(요청) { 반환 화면["방명록화면"](자료["글모두"]()) }

함수 등록(요청) {
    이름 = 값얻기(요청["질의"], "이름", "익명")
    번호 = 자료["글넣기"](이름, 값얻기(요청["질의"], "내용", ""))
    반환 화면["등록화면"](이름, 번호)
}

서버(숫자(환경변수("포트", "8000")), {"/": 첫화면, "/등록": 등록})
