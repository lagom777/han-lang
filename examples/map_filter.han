# 고차 내장함수 — 변환(map) · 거르기(filter)
수들 = [1, 2, 3, 4, 5, 6]

짝수 = 거르기(수들, 람다(n) { 반환 n % 2 == 0 })
출력("짝수: " + 짝수)

제곱들 = 변환(수들, 람다(n) { 반환 n * n })
출력("제곱: " + 제곱들)

# 조합: 짝수만 골라 제곱
출력("짝수의 제곱: " + 변환(짝수, 람다(n) { 반환 n * n }))
