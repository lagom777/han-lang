# 묶음 — 목록을 크기씩 나누기 (배치/그리드 행)
숫자 = [1, 2, 3, 4, 5, 6, 7]

# 3개씩 묶기
반복 i, 묶 를 묶음(숫자, 3) 에서 {
    출력("그룹 " + (i + 1) + ": " + 묶)
}

# 이름을 2개씩 줄 나눔
이름 = ["철수", "영희", "민수", "지영", "현우"]
출력("")
반복 줄 를 묶음(이름, 2) 에서 {
    출력(합치기(줄, " | "))
}
