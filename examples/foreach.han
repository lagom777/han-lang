# 반복 — 목록/사전 순회 (반복 ... 에서)
과일 = ["사과", "바나나", "포도"]
반복 f 를 과일 에서 {
    출력("- " + f)
}

점수 = {"국어": 90, "수학": 85}
반복 과목 를 점수 에서 {
    출력(과목 + ": " + 점수[과목] + "점")
}

수들 = [3, 5, 7]
합계 = 0
반복 n 를 수들 에서 {
    합계 = 합계 + n
}
출력("합계: " + 합계)
