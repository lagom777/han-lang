# JSON — LLM 출력·API 응답 다루기 (AI 개발 핵심)

# 1) JSON 문자열 → 값
응답 = 제이슨파싱("{\"이름\": \"한\", \"버전\": 0, \"기능\": [\"람다\", \"JSON\"]}")
출력("이름: " + 응답["이름"])
출력("기능 수: " + 길이(응답["기능"]))

# 2) 값 → JSON 문자열 (들여쓰기 2칸으로 보기 좋게)
설정 = {"모델": "han", "온도": 0.7, "도구": ["검색", "계산"]}
출력(제이슨문자열(설정, 2))
