# 함수 기본 인자 (기본값 있는 매개변수)
함수 인사(이름, 말="안녕하세요") {
    반환 서식("{}, {}님!", 말, 이름)
}
출력(인사("한"))               # 안녕하세요, 한님!
출력(인사("한", "반가워요"))   # 반가워요, 한님!

# AI 호출에 기본 모델 지정
함수 물어보기(질문내용, 모델="gpt-4o-mini") {
    반환 서식("[{}] {}", 모델, 질문내용)
}
출력(물어보기("오늘 날씨는?"))
출력(물어보기("오늘 날씨는?", "claude-3.5"))
