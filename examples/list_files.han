# 파일목록 — 폴더 안 파일 나열·필터 (일괄 처리)
파일쓰기("/tmp/han_data_1.txt", "첫째")
파일쓰기("/tmp/han_data_2.txt", "둘째")

전체 = 파일목록("/tmp")
내것 = 거르기(전체, 람다(f) { 반환 시작(f, "han_data_") })

출력("han_data_ 파일 " + 길이(내것) + "개:")
반복 f 를 정렬(내것) 에서 {
    출력(" - " + f)
}
