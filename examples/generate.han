# 생성 — 함수로 목록 만들기 (Array.from)
제곱들 = 생성(6, 람다(i) { 반환 i * i })
출력(제곱들)   # [0, 1, 4, 9, 16, 25]

# 구구단 3단
출력("3단:")
반복 줄 를 생성(9, 람다(i) { 반환 "3 x " + (i + 1) + " = " + (3 * (i + 1)) }) 에서 {
    출력(줄)
}
