# 사이값 — 값을 범위로 제한 (clamp)
출력(사이값(150, 0, 100))   # 100 (상한)
출력(사이값(-20, 0, 100))   # 0 (하한)
출력(사이값(42, 0, 100))    # 42 (그대로)

# 점수를 0~100으로 정규화
점수 = [120, 85, -10, 50]
정규화 = 변환(점수, 람다(s) { 반환 사이값(s, 0, 100) })
출력(정규화)   # [100, 85, 0, 50]
