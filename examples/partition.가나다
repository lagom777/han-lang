# 분할 — 조건으로 두 그룹 나누기 (합격/불합격)
점수 = [85, 40, 90, 55, 70]
결과 = 분할(점수, 람다(s) { 반환 s >= 60 })

출력("합격: " + 결과[0])    # [85, 90, 70]
출력("불합격: " + 결과[1])  # [40, 55]
출력("합격률: " + 반올림(길이(결과[0]) / 길이(점수) * 100, 0) + "%")
