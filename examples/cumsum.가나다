# 누적합 — running total (월별 누적 매출 등)
월매출 = [100, 150, 80, 200]
누적 = 누적합(월매출)

출력("월별 매출: " + 월매출)
출력("누적 매출: " + 누적)        # [100, 250, 330, 530]
출력("연 총합: " + 합(월매출))     # 530
출력("누적 곱(1~4): " + 누적곱([1, 2, 3, 4]))  # [1, 2, 6, 24]
