# 빈도 — 원소별 등장 횟수 세기 (단어 빈도)
단어들 = ["사과", "배", "사과", "감", "배", "사과"]
세기 = 빈도(단어들)
출력(세기)

# 가장 많이 나온 것
반복 항목 를 항목들(세기) 에서 {
    과일, 횟수 = 항목
    출력(과일 + ": " + 횟수 + "회")
}
출력("최다: " + 최대기준(항목들(세기), 람다(쌍){ 반환 쌍[1] })[0])
