# 회문 판별 — 거꾸로 문자열 활용 (문자열도 뒤집어 문자열 반환)
함수 회문인가(말) {
    반환 말 == 거꾸로(말)
}

반복 말 를 ["기러기", "토마토", "안녕", "level"] 에서 {
    만약 회문인가(말) {
        출력(말 + " → 회문 ✓")
    } 아니면 {
        출력(말 + " → 회문 아님")
    }
}
