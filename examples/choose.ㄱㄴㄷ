# 고르기(조건, 참값, 거짓값) — 삼항 조건식 대용. 선택된 가지만 평가(지연).
반복 점수 를 [95, 82, 70, 55] 에서 {
  등급 = 고르기(점수 >= 90, "A", 고르기(점수 >= 80, "B", 고르기(점수 >= 60, "C", "F")))
  출력(점수 + "점 → " + 등급)
}

# 지연 평가 — 0이면 나누기를 아예 하지 않아 안전
수 = 0
출력("역수: " + 고르기(수 != 0, 100 / 수, "정의 안 됨"))
