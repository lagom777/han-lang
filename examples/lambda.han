# 람다(익명 함수)와 고차 함수
제곱 = 람다(수) { 반환 수 * 수 }
출력("7의 제곱 = " + 제곱(7))

# 목록의 각 원소에 함수 적용(고차 함수)
함수 각각적용(목록, f) {
    결과 = []
    반복 x 를 목록 에서 {
        추가(결과, f(x))
    }
    반환 결과
}

출력(각각적용([1, 2, 3, 4], 람다(n) { 반환 n * 10 }))
출력(각각적용(["가", "나"], 람다(s) { 반환 s + "!" }))
