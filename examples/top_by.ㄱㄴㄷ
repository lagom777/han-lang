# 상위기준 — 키 기준 상위 N (객체 순위표)
직원 = [
    {"이름": "김", "실적": 85},
    {"이름": "이", "실적": 92},
    {"이름": "박", "실적": 78},
    {"이름": "최", "실적": 95}
]

출력("실적 우수 2명:")
반복 순위, e 를 상위기준(직원, 2, 람다(x) { 반환 x["실적"] }) 에서 {
    출력((순위 + 1) + "위 " + e["이름"] + " (" + e["실적"] + ")")
}
