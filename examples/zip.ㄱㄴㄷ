# 묶기(zip) — 두 목록을 짝지어 묶기
이름들 = ["민지", "현우", "서연"]
점수들 = [85, 92, 78]

반복 쌍 를 묶기(이름들, 점수들) 에서 {
    출력(서식("{}: {}점", 쌍[0], 쌍[1]))
}

# 묶기 + 변환: 두 목록 원소별 합
출력("원소별 합: " + 변환(묶기([1, 2, 3], [10, 20, 30]), 람다(쌍) { 반환 쌍[0] + 쌍[1] }))
