# 숫자인가 — 입력·데이터가 숫자로 변환 가능한지 검증 후 안전하게 숫자로 변환.
입력들 = ["100", "3.14", "abc", "  -5 ", "12개", ""]

반복 값 를 입력들 에서 {
  만약 숫자인가(값) {
    출력("숫자 OK: " + 값 + " → " + 숫자(값))
  } 아니면 {
    출력("숫자 아님: '" + 값 + "'")
  }
}
