# 병합 — 사전 합치기 (뒤가 우선, 설정/기본값에 유용)
기본값 = {"모델": "han", "온도": 0.7, "최대토큰": 100}
사용자설정 = {"온도": 0.2}

설정 = 병합(기본값, 사용자설정)
출력("모델: " + 설정["모델"])
출력("온도: " + 설정["온도"])        # 사용자값 0.2 (덮어씀)
출력("최대토큰: " + 설정["최대토큰"])  # 기본값 100 유지
