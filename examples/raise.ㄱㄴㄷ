# 발생 — 직접 오류 던지기 (시도/잡기와 짝)
함수 나이검증(나이) {
    만약 나이 < 0 {
        발생("나이는 음수일 수 없습니다")
    }
    반환 나이
}

시도 {
    출력("검증된 나이: " + 나이검증(25))
    출력("검증된 나이: " + 나이검증(-5))    # 발생 → 잡기로
} 잡기(오류) {
    출력("검증 실패: " + 오류)
}
출력("프로그램 계속")
