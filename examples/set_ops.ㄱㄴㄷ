# 집합 연산 — 목록을 집합처럼
가 = [1, 2, 3, 4, 5]
나 = [4, 5, 6, 7]
출력("교집합: " + 교집합(가, 나))
출력("합집합: " + 합집합(가, 나))
출력("차집합(가-나): " + 차집합(가, 나))

# 문자열 목록에도 동작 + 중복 제거
과일1 = ["사과", "배", "감", "배"]
과일2 = ["배", "감", "귤"]
출력("공통 과일: " + 교집합(과일1, 과일2))
