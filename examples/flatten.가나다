# 평탄화 — 중첩 목록을 한 단계 펼치기
묶음들 = [[1, 2], [3, 4], [5, 6]]
출력(평탄화(묶음들))    # [1, 2, 3, 4, 5, 6]

# 변환이 목록을 돌려줄 때 펼치기(map → flatten)
문장 = ["가 나", "다 라 마", "바"]
단어 = 평탄화(변환(문장, 람다(s) { 반환 나누기(s, " ") }))
출력(단어)
출력("총 단어 수: " + 길이(단어))
