# AI 표준 라이브러리 — 질문() / 체계질문()
# OPENROUTER_API_KEY 를 설정하면 실제 LLM 답변, 없으면 안내가 나와요.

답 = 질문("한국어로 짧게 인사해줘")
출력(답)

# 모델 지정도 가능: 질문("...", "anthropic/claude-3.5-sonnet")

# 체계질문 — 시스템 프롬프트로 AI 역할/태도 지정
시 = 체계질문("너는 간결한 시인이다. 한 줄로만 답한다.", "별을 노래해줘")
출력(시)
