# 구조 분해 대입 — 가, 나 = 목록
가, 나 = [10, 20]
출력(가 + 나)              # 30

# 항목들과 조합: 키-값 쌍 분해
점수표 = {"국어": 90, "수학": 85}
반복 쌍 를 항목들(점수표) 에서 {
    과목, 점수 = 쌍
    출력(서식("{}: {}", 과목, 점수))
}

# 목록 반환 함수 → 바로 분해
함수 최소최대(목록) { 반환 [최소(목록), 최대(목록)] }
하한, 상한 = 최소최대([3, 7, 1, 9, 4])
출력(서식("범위 {}~{}", 하한, 상한))
