# 사전 안전 접근 — 값얻기(기본값)·키있나
설정 = {"이름": "홍길동", "레벨": 5}

출력("이름: " + 값얻기(설정, "이름", "익명"))
출력("점수: " + 값얻기(설정, "점수", 0))      # 없는 키 → 기본값 0 (오류 없음)

만약 키있나(설정, "레벨") {
    출력("레벨: " + 설정["레벨"])
}
만약 키있나(설정, "직업") {
    출력("직업: " + 설정["직업"])
} 아니면 {
    출력("직업 정보 없음")
}
