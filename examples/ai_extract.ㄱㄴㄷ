# 추출 — 텍스트에서 지시한 정보만 뽑기. OPENROUTER_API_KEY 있으면 실제 추출, 없으면 안내 stub.
글 = "홍길동 고객님, 주문번호 A-2024-0917 건은 3월 5일 배송 예정입니다."

출력("주문번호: " + 추출(글, "주문번호"))
출력("배송일: " + 추출(글, "배송 예정일"))
출력("고객명: " + 추출(글, "고객 이름"))
