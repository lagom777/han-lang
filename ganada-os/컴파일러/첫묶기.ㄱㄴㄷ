# 정본 컴파일러 초안 (한글만).
# 가상에 열기·읽기·쓰기·묶음이 열렸다.
# 이 파일이 .ㄱㄴㄷ → .ㄱㅂ 를 만들면 사다리/묶기를 지운다.
#
# 핵은 숫자와 기호.
#   = += + - * / % == != < > { } ( ) ,
#   만약 아니면 동안 출력 묶음 돌려주기
#   열기 만들기 읽기 쓰기 닫기

출력("가나다 묶기 — 숫자와 기호")
출력("소스=.ㄱㄴㄷ  바이트=.ㄱㅂ  실행기=가상")
