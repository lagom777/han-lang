# 정본 컴파일러 초안 (한글만).
# 지금은 시드 사다리(사다리/묶기)가 같은 일을 대신 수행한다.
# 가상에 열기·읽기·쓰기가 열리면 이 파일이 .ㄱㄴㄷ → .ㄱㅂ 를 만든다.
#
# 핵은 숫자와 기호. 이 파일이 묶는 말도 그것이다.
#   = + - * / == != < > { } ( )
#   만약 아니면 동안 출력
#
# 아직 가상이 이 프로그램 전체를 실행하지 못한다 (파일 칸이 없음).

출력("가나다 묶기 — 숫자와 기호")
출력("소스=.ㄱㄴㄷ  바이트=.ㄱㅂ  실행기=가상")
